ktor {
    deployment {
        port = ${PORT}
        watch = [ com.ianarbuckle.conferencesapi.ApplicationKt.module ]
    }
    application {
        modules = [ com.ianarbuckle.conferencesapi.ApplicationKt.module ]
    }
    mongoUri = ${MONGO_URI}
    ktor.deployment.port = ${PORT}
}
